coverage:
  status:
    project:
      default:
        target: auto
        threshold: 2%
    patch:
      default:
        target: 80%

codecov:
  notify:
    after_n_builds: 2

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: false
